<view class="page">
  <view class="codex-intro muted">6 只各有性格的小宠。你的本命宠带「本命」徽。每只从蛋到童年是同一只；长成少年时，由你亲手选它的样子（4 选 1，和你怎么照顾无关）。企鹅 / 熊 / 海豹的 3 种形态是真实的近缘物种。</view>
  <view wx:for="{{lines}}" wx:for-item="line" wx:key="key" class="linecard {{line.bonded ? 'linecard--bonded' : ''}}">
    <view class="line-head">
      <text class="line-name">{{line.name}}</text>
      <text wx:if="{{line.bonded}}" class="line-tag">本命</text>
    </view>
    <view class="forms">
      <view wx:for="{{line.forms}}" wx:for-item="f" wx:key="id" class="form {{f.isTrue ? 'form--true' : ''}}">
        <image class="form-sprite" src="{{f.sprite}}" mode="aspectFit" />
        <text class="form-label">{{f.label}}</text>
        <text class="form-lean">{{f.isTrue ? '本形' : '可选'}}</text>
      </view>
    </view>
  </view>
</view>
